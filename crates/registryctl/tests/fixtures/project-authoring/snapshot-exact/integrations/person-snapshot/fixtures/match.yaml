name: snapshot-match
classification: synthetic
input: { person_id: PER-00000001 }
interactions:
  - expect: { method: GET, path: /snapshot }
    respond: { status: 200, body: { registration_status: active, eligible: true } }
expect:
  outcome: match
  outputs: { registration_status: active, eligible: true }
  claims:
    population-record-exists: true
    benefits-status: active
    benefits-eligible: true
    emergency-record-exists: true
    emergency-status: redacted
