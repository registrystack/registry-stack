name: snapshot-match
classification: synthetic
input: { person_id: PER-00000001 }
interactions:
  - expect: { method: GET, path: /snapshot }
    respond: { status: 200, body: { registration_status: active, residency_confirmed: true } }
expect:
  outcome: match
  outputs: { registration_status: active, residency_confirmed: true }
