name: snapshot-ambiguous
classification: synthetic
input: { person_id: PER-00000003 }
interactions:
  - expect: { method: GET, path: /snapshot }
    respond:
      status: 200
      body:
        - { registration_status: active, eligible: true }
        - { registration_status: inactive, eligible: false }
expect:
  outcome: ambiguous
  outputs: {}
  claims: {}
