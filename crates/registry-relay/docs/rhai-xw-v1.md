<!-- Generated from crates/registry-relay/src/rhai_worker/xw.rs. Do not edit by hand. -->

# Relay Rhai `xw.v1` function reference

`xw.v1` is available to every reviewed script source adapter. Helpers are deterministic and product-neutral. They have no network, filesystem, credential, environment, clock, random, or logging capability. Date calculations require every date explicitly, including the reference date for age calculations.

| Signature | Accepted types | Null behavior | Bounds | Failure behavior | Example |
| --- | --- | --- | --- | --- | --- |
| `xw.text.trim(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.trim("  Ada  ")` |
| `xw.text.lower_ascii(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.lower_ascii("ABC-123")` |
| `xw.text.upper_ascii(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.upper_ascii("abc-123")` |
| `xw.text.title_simple(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.title_simple("ada lovelace")` |
| `xw.text.normalize_space(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.normalize_space("  Ada   Lovelace  ")` |
| `xw.text.remove_accents(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.remove_accents("Crème Brûlée")` |
| `xw.text.slug(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.text.slug("Hello, WORLD!")` |
| `xw.date.parse_date(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.parse_date("2024-02-29")` |
| `xw.date.parse_date(value: string, pattern: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.parse_date("29/02/2024", "%d/%m/%Y")` |
| `xw.date.format_date_or_datetime(value: string, pattern: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.format_date_or_datetime("2024-02-29", "%d/%m/%Y")` |
| `xw.date.age_on(birth_date: string, reference_date: string) -> int` | string, string → int | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.age_on("2000-05-27", "2026-05-26")` |
| `xw.date.years_between(start: string, end: string) -> int` | string, string → int | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.years_between("2020-01-01", "2024-01-01")` |
| `xw.date.days_between(start: string, end: string) -> int` | string, string → int | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.days_between("2024-01-01", "2024-01-03")` |
| `xw.date.add_days(value: string, days: int) -> string` | string, int → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.add_days("2024-01-01", 2)` |
| `xw.date.add_months(value: string, months: int) -> string` | string, int → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.add_months("2024-01-31", 1)` |
| `xw.date.start_of_month(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.start_of_month("2024-02-15")` |
| `xw.date.end_of_month(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.end_of_month("2024-02-15")` |
| `xw.date.min_date(left: string, right: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.min_date("2024-01-01", "2024-02-01")` |
| `xw.date.max_date(left: string, right: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | Invalid, unsupported, or out-of-range dates reject the evaluation with a value-free contract violation. | `xw.date.max_date("2024-01-01", "2024-02-01")` |
| `xw.ids.stable_hash_sha256(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.ids.stable_hash_sha256("subject-123")` |
| `xw.ids.stable_hash_sha256(value: string, non_secret_salt: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.ids.stable_hash_sha256("subject-123", "project-v1")` |
| `xw.ids.prefixed_slug(prefix: string, value: string) -> string` | string, string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.ids.prefixed_slug("person", "Ada Lovelace")` |
| `xw.ids.clean_id(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.ids.clean_id("a b_c-d!")` |
| `xw.json.parse_json(value: string) -> any` | string → any JSON value | Null is not accepted for any argument. | Uses the worker's string, array, map, expression-depth, node-count, and frame-oriented data limits. | Invalid JSON, unsupported values, non-interoperable numbers, or exceeded bounds reject the evaluation with a value-free contract violation or budget failure. | `xw.json.parse_json("{\"active\":true}")` |
| `xw.json.stringify_json(value: any) -> string` | any JSON value → string | Null is not accepted for any argument. | Uses the worker's string, array, map, expression-depth, node-count, and frame-oriented data limits. | Invalid JSON, unsupported values, non-interoperable numbers, or exceeded bounds reject the evaluation with a value-free contract violation or budget failure. | `xw.json.stringify_json(#{ active: true })` |
| `xw.email.normalize_email(value: string) -> string` | string → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.email.normalize_email(" USER@Example.ORG ")` |
| `xw.email.email_domain(value: string) -> string?` | string → string or null | Returns null when the requested value is absent; arguments do not accept null. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.email.email_domain("user@example.org")` |
| `xw.email.is_valid_email(value: string) -> bool` | string → bool | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.email.is_valid_email("user@example.org")` |
| `xw.redaction.mask(value: string, visible_last: int) -> string` | string, int → string | Null is not accepted for any argument. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.redaction.mask("123456789", 4)` |
| `xw.redaction.redact() -> string` | none → string | No arguments or null behavior. | Every input and returned string is bounded by worker.limits.max_string_bytes. | No helper-defined failure for values within worker bounds. | `xw.redaction.redact()` |

The callable table above is generated from the same catalog that registers functions in the Relay worker. `today`, one-argument `age_on`, `parse_datetime`, regex, phone, filesystem, clock, random, network, and logging helpers are not part of `xw.v1`.
