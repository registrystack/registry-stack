# Tests config.validation_error: env var is set but value is not a
# valid sha256 API key fingerprint.
server:
  bind: 127.0.0.1:8080
catalog:
  title: t
  base_url: https://example.test
  publisher: p
vocabularies: {}
auth:
  mode: api_key
  api_keys:
    - id: k1
      fingerprint:
        provider: env
        name: TEST_KEY_HASH_BAD_FINGERPRINT
      scopes:
        - registry_relay:admin
audit:
  sink: stdout
datasets: []
