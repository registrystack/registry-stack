meta {
  name: Missing credential (expects 401)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/datasets/benefits_casework/entities/household/records?district=riverbend
  body: none
  auth: none
}

docs {
  Attempts to read a protected endpoint with no Authorization header at all.
  expected: 401 auth.missing_credential
  Confirms that unauthenticated requests are rejected at the auth middleware layer.
}
