# `@registrystack/evidence-client-linux-arm64-gnu`

This package contains the **aarch64-unknown-linux-gnu** native binary for
`@registrystack/evidence-client`. Install the root package instead of depending
on this platform package directly.
