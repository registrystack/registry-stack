# `@registrystack/relay-client-linux-x64-gnu`

This package contains the **x86_64-unknown-linux-gnu** native binary for
`@registrystack/relay-client`. Install the root package instead of depending on
this platform package directly.
