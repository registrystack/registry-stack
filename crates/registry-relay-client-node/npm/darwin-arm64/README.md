# `@registrystack/relay-client-darwin-arm64`

This package contains the **aarch64-apple-darwin** native binary for
`@registrystack/relay-client`. Install the root package instead of depending on
this platform package directly.
