openapi: 3.0.3
info:
  title: External Ref
  version: "1.0.0"
paths:
  /records:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: 'external.yaml#/components/schemas/Record'
