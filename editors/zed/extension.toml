id = "registry-stack"
name = "Registry Stack"
version = "0.1.0"
schema_version = 1
authors = ["Registry Stack contributors"]
description = "Cross-file navigation and reference diagnostics for Registry Stack YAML projects."
repository = "https://github.com/registrystack/registry-stack"

[language_servers.registry-stack]
name = "Registry Stack Language Server"
languages = ["YAML"]
