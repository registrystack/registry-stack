StylesPath = styles
MinAlertLevel = suggestion
Vocab = RegistryDocs

[formats]
mdx = md

[*.md]
BasedOnStyles = Vale, RegistryDocsBase, RegistryDocs
Vale.Terms = NO
Vale.Spelling = NO

[*.mdx]
BasedOnStyles = Vale, RegistryDocsBase, RegistryDocs
Vale.Terms = NO
Vale.Spelling = NO
TokenIgnores = (?s)---.*?---, (?s)\{/\*.*?\*/\}, `[^`]*`, (?s)<[^>]+>, import .*

# Formal specification layer. These documents use RFC 2119 keywords (MUST,
# SHOULD, MAY), so FuturePromises is disabled here: "the relay SHOULD support X"
# is normative language, not an unkept promise. RegistryDocsSpec replaces it
# with conformance-honesty checks. MetaNarration is disabled because RS-DOC
# specifies the docs site itself, so naming a sidebar group is its subject, not
# narration. TokenIgnores is repeated because a path section's settings do not
# inherit the format section's keys.
[src/content/docs/spec/**]
BasedOnStyles = Vale, RegistryDocsBase, RegistryDocs, RegistryDocsSpec
RegistryDocsBase.FuturePromises = NO
RegistryDocs.MetaNarration = NO
Vale.Terms = NO
Vale.Spelling = NO
TokenIgnores = (?s)---.*?---, (?s)\{/\*.*?\*/\}, `[^`]*`, (?s)<[^>]+>, import .*
