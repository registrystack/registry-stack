---
title: Registry Server configuration reference
description: Generated fields, accepted values, defaults, and constraints for Registry Server projects, modules, and runtime configuration.
status: draft
owner: registry-docs
source_repos:
  - registry-stack
last_reviewed: unreviewed
doc_type: reference
locale: en
standards_referenced:
  - json-schema
wide: true
---

import ConfigurationReference from '../../../components/ConfigurationReference.astro';
import reference from '../../../data/generated/server-configuration.json';

Look up configuration keys for `registry.yaml`, `module.yaml`, and `runtime.yaml`.
The tables are generated from Registry Server's Rust-derived JSON Schemas, including event
conditions, webhook destinations, access profiles, and module extensions.

## Scope

This reference describes the source-preview configuration, not a released or frozen contract.
Types, required fields, accepted values, defaults, and constraints come from the schemas.
Descriptions appear where the Rust definition supplies them.
Internal compatibility fields and test-journey documents are outside this reference.

Use [configure your registry](../../configure/registry-server/) for worked configurations and
deployment steps. A schema-valid document still needs the compiler and runtime checks:
`registry-serverctl check <project>` validates the combined model;
`registry-serverctl doctor --runtime-config <absolute-file>` checks deployment configuration.
Cross-field rules and references to other files cannot all be expressed by these tables.

{/* Evidence: crates/registry-server/src/schema.rs, documents() and runtime_documents();
    crates/registry-server/src/contract.rs, RegistryProject and RegistryModule;
    crates/registry-serverctl/src/lib.rs, DoctorArgs and check(). */}

## Read the tables

`fields[]` means an array item; `eventDestinations.*` means a map entry whose name you choose.
Required fields inside an optional object apply when that object is present.
Conditional fields depend on the selected variant or another configuration value.
Accepted values list the union across variants; constraints separated by `or` are alternatives.
A schema default applies only where that variant declares it. `Not specified` does not mean
that every value is accepted.

For example, `events[].when.afterEquals.*` holds a comparison value for a field you name.
The schema permits a scalar or null; the compiler also checks that the value matches the
declared field type and that the condition is valid for the event trigger.

{/* Evidence: crates/registry-server/src/contract.rs, EventScalarValue and EventConditionSource;
    crates/registry-server/src/compiler.rs;
    docs/site/scripts/configuration-reference.mjs, collectFields(). */}

## Regeneration

The source schemas live under `products/registry-server/generated/authoring/` and
`products/registry-server/generated/runtime/`.
Their owning generators are the `authoring-schema` and `runtime-schema` Rust examples.
`products/registry-server/scripts/check-generated.sh` checks that they match the source types.
Run `npm run generate` in `docs/site` to rebuild these tables from those schemas.

`registry-serverctl generate schemas <project> --output <directory>` has a different purpose:
it generates record schemas for that project's API, not configuration schemas.

{/* Generated from src/data/generated/server-configuration.json by
    docs/site/scripts/generate-server-configuration.mjs. Run npm run generate from docs/site. */}

<ConfigurationReference contracts={reference.contracts} prefix="server" />

## Next

- [Configure your registry](../../configure/registry-server/) for models, permissions, and webhooks.
- [Events and webhooks](../registry-server-events/) for payloads, signatures, retries, and replay.
- [Create and query your first registry](../../tutorials/first-registry-server/) to try the API.
- [Registry Server overview](../../explanation/configuration-defined-registry/) for the product boundaries.
