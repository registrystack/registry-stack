---
title: Registry Stack docs
description: "Documentation for Registry Stack: protected registry APIs, governed evidence exchange, Registry Relay, and Registry Notary."
status: current
owner: registry-docs
source_repos:
  - registry-relay
  - registry-notary
last_reviewed: "2026-07-27"
doc_type: explanation
locale: en
standards_referenced: []
---

Registry Stack adds registry-facing services, protected reads,
[governed evidence](reference/glossary/), and audit
records, over data an institution already holds.
The source remains authoritative; only an authorized, minimized result crosses
the reviewed boundary.

Start with [your first registry API](tutorials/publish-spreadsheet-secured-registry-api/),
then [use your own spreadsheet](tutorials/use-your-spreadsheet/) or
[evaluate a claim](tutorials/verify-claim-registry-api/) from a canonical
offline fixture.

## Two products

Registry Relay protects what an institution already holds. Registry Notary certifies answers drawn
from that data.
Use the local tutorials below for the current first-run path.

### Registry Relay

Registry Relay exposes protected, scoped, read-only APIs over existing sources: files, extracts,
databases, and legacy registry systems.

[Your first registry API](tutorials/publish-spreadsheet-secured-registry-api/): run a protected
API locally over a sample spreadsheet.

### Registry Notary

Registry Notary evaluates and attests governed evidence: bounded claim results,
credential issuance, disclosure policy, and audit provenance.

[Your first claim check](tutorials/verify-claim-registry-api/): evaluate
synthetic match and no-match claim fixtures, then build validated unsigned
Relay and Notary inputs.

## Security

Registry Stack authorization is scope-based and deny-by-default.
Every request writes to a hash-chained audit envelope. Health and readiness probes are excluded
by default.
These docs are evidence-anchored: every claim points to code, a fixture, a test, or a
specification requirement ID.

Read the [security overview](security/) for the threat model and the public evidence a reviewer can
check.
