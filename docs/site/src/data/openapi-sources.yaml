- id: registry-evidence
  name: Evidence Gateway API
  owner: registry-evidence
  source: Pulled from `products/evidence/generated/registry-evidence.openapi.json` at the pinned ref in `src/data/repo-docs.yaml` by `scripts/fetch-openapi.mjs`. The document is generated by `cargo run -p registry-evidence --example evidence-contracts` and byte-drift-checked in root CI by the `evidence-contracts` job.
  artifact: openapi/registry-evidence.openapi.json
  status: pulled at the pinned ref (build artifact, regenerated each build). A running Evidence Gateway service publishes the same generated document at `GET /openapi.json` with no authentication required.
  reference_path: /reference/apis/evidence/
