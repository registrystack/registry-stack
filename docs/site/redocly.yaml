extends:
  - recommended
apis:
  registry-relay:
    root: openapi/registry-relay.openapi.json
  registry-notary:
    root: openapi/registry-notary.openapi.json
  registry-evidence:
    root: openapi/registry-evidence.openapi.json
rules:
  no-empty-servers: off
  no-unused-components: warn
  operation-operationId: warn
  security-defined: warn
