<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 632" role="img"
     font-family="'Public Sans', system-ui, -apple-system, BlinkMacSystemFont, sans-serif"
     text-rendering="geometricPrecision">
  <title>Registry Stack trust boundaries</title>
  <desc>One request path runs downward from Registry Mint, which issues a short-lived access token, to the caller, to Evidence Gateway, and to Evidence Gateway's configured authoritative source, which it reaches over a fixed HTTP request or a read-only SQLite extract. Registry Relay has a separate protected-read path to its own registry sources. The dashed rule above Evidence Gateway marks the authenticated service edge; the dashed rule below it marks the source edge, which is authenticated only when the source is HTTP, since a statement source has no origin, no credential, and no network hop. Caller to Evidence Gateway carries a bearer token, a declared purpose, a named requirement revision, and selector values, and returns the answer rather than the record. Evidence Gateway source access is one fixed source request per acquisition stage, fixed by trusted configuration and executed only by the core: an HTTP source fixes origin, method, path, headers, authentication, TLS, projection, and redirect and proxy denial, and a statement source fixes one reviewed SQL statement over a local read-only extract with no origin, no credential, and no network hop. Lookup collapses to match, no match, or ambiguous, and raw responses are never logged. Registry Mint to Evidence Gateway runs one way: Evidence Gateway verifies tokens as an ordinary resource server and does not depend on Registry Mint. A separate lane runs operator to signing key material to Evidence Gateway to the published key set to the verifier, which pins that key set out of band. The footnote lists the unauthenticated operational and discovery routes at the Evidence Gateway edge.</desc>

  <text x="16" y="22" fill="#6a6a6a" font-size="11" font-weight="700" letter-spacing="0.08em">TRUST BOUNDARIES</text>
  <rect x="16" y="30" width="32" height="2" fill="#000091"/>

  <g fill="#ffffff" stroke="#161616" stroke-width="1">
    <rect x="16" y="52" width="170" height="40"/>
    <rect x="16" y="126" width="170" height="40"/>
    <rect x="16" y="232" width="170" height="52"/>
    <rect x="16" y="350" width="170" height="52"/>
    <rect x="16" y="440" width="170" height="40"/>
  </g>

  <g fill="#161616" font-size="12.5" font-weight="700">
    <text x="28" y="71">Registry Mint</text>
    <text x="28" y="145">Caller</text>
    <text x="28" y="252">Evidence Gateway</text>
    <text x="28" y="370">Authoritative source</text>
    <text x="28" y="459">Registry Relay</text>
  </g>

  <g fill="#6a6a6a" font-size="9.5"
     font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace">
    <text x="28" y="85">registry-mint</text>
    <text x="28" y="266">registry-evidence</text>
    <text x="28" y="473">registry-relay</text>
  </g>

  <g fill="#6a6a6a" font-size="9">
    <text x="28" y="159">the client asking the question</text>
    <text x="28" y="279">one bounded question</text>
    <text x="28" y="384">fixed HTTP request or</text>
    <text x="28" y="396">read-only SQLite extract</text>
  </g>

  <g stroke="#000091" stroke-width="1" fill="none">
    <line x1="101" y1="94" x2="101" y2="124"/>
    <polyline points="97,118 101,124 105,118"/>
    <line x1="101" y1="168" x2="101" y2="230"/>
    <polyline points="97,224 101,230 105,224"/>
    <line x1="101" y1="286" x2="101" y2="348"/>
    <polyline points="97,342 101,348 105,342"/>
  </g>

  <g stroke="#161616" stroke-width="1.2" stroke-dasharray="6 4">
    <line x1="10" y1="184" x2="194" y2="184"/>
    <line x1="10" y1="302" x2="194" y2="302"/>
  </g>

  <g fill="#000091" font-size="9" font-weight="700" letter-spacing="0.04em">
    <text x="10" y="180">SERVICE EDGE</text>
    <text x="10" y="298">SOURCE EDGE</text>
  </g>

  <g fill="#6a6a6a" font-size="9.5"
     font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace">
    <text x="124" y="113">short-lived access token</text>
    <text x="124" y="204">bearer token, purpose,</text>
    <text x="124" y="217">revision, selectors</text>
    <text x="124" y="322">one fixed source request</text>
    <text x="124" y="335">per acquisition stage</text>
    <text x="124" y="424">separate product path</text>
  </g>

  <g fill="#000091" font-size="10" font-weight="700" letter-spacing="0.04em">
    <text x="270" y="66">REGISTRY MINT TO EVIDENCE GATEWAY</text>
    <text x="270" y="128">CALLER TO EVIDENCE GATEWAY</text>
    <text x="270" y="209">THE SERVICE EDGE (AUTHENTICATION)</text>
    <text x="270" y="277">EVIDENCE TO ITS FIXED SOURCES</text>
    <text x="270" y="358">PRODUCT BOUNDARIES STAY SEPARATE</text>
    <text x="270" y="439">RELAY'S SEPARATE SOURCE BOUNDARY</text>
  </g>

  <g fill="#3a3a3a" font-size="11">
    <text x="270" y="80">One way. Registry Mint issues the token; Evidence Gateway verifies it as</text>
    <text x="270" y="93">an ordinary resource server, knowing it as an issuer URL and a key</text>
    <text x="270" y="106">set. A deployment with an identity provider does not run Mint at all.</text>

    <text x="270" y="142">A bearer token, a declared purpose, a named requirement revision,</text>
    <text x="270" y="155">and selector values go in; the answer, not the record, comes back.</text>
    <text x="270" y="168">Only predefined revisions are evaluable, one authorization decision</text>
    <text x="270" y="181">binds the whole tuple, and possession is never authority.</text>

    <text x="270" y="223">Every record- or assertion-bearing route is authenticated before</text>
    <text x="270" y="236">responding. Evidence Gateway runs one OIDC bearer profile, one trusted</text>
    <text x="270" y="249">issuer, one principal claim, and no trust in proxy identity headers.</text>

    <text x="270" y="291">Trusted configuration fixes each source and only the core executes it. An HTTP</text>
    <text x="270" y="304">source fixes origin, method, path, headers, authentication, TLS, projection, and</text>
    <text x="270" y="317">redirect and proxy denial. A statement source fixes one reviewed SQL statement</text>
    <text x="270" y="330">over a local read-only extract: no origin, no credential, no network hop. Lookup</text>
    <text x="270" y="343">collapses to match, no match, or ambiguous; raw responses are never logged.</text>

    <text x="270" y="372">Evidence Gateway and Relay use independently configured sources.</text>
    <text x="270" y="385">Neither product calls or authorizes the other. Each keeps its own</text>
    <text x="270" y="398">authorization decisions, audit trail, and readiness checks; only</text>
    <text x="270" y="411">Evidence Gateway ever holds a source credential.</text>

    <text x="270" y="453">Relay owns its own source boundary: one local SQLite file opened</text>
    <text x="270" y="466">read-only, with no origin, no credential, and no outbound request.</text>
  </g>

  <line x1="0" y1="496" x2="720" y2="496" stroke="#e5e5e5" stroke-width="1"/>

  <text x="16" y="516" fill="#000091" font-size="10" font-weight="700" letter-spacing="0.04em">OPERATOR TO SIGNING KEY MATERIAL, AND CONSUMER TO THE PUBLISHED KEY SET</text>

  <g fill="#ffffff" stroke="#161616" stroke-width="1">
    <rect x="16" y="528" width="96" height="34"/>
    <rect x="160" y="528" width="120" height="34"/>
    <rect x="344" y="528" width="96" height="34"/>
  </g>

  <g fill="#161616" font-size="12" font-weight="700">
    <text x="28" y="549">Operator</text>
    <text x="170" y="549" font-size="10.5">Evidence Gateway</text>
    <text x="356" y="549">Verifier</text>
  </g>

  <g stroke="#000091" stroke-width="1" fill="none">
    <line x1="112" y1="545" x2="158" y2="545"/>
    <polyline points="152,541 158,545 152,549"/>
    <line x1="280" y1="545" x2="342" y2="545"/>
    <polyline points="336,541 342,545 336,549"/>
  </g>

  <g fill="#6a6a6a" font-size="9.5"
     font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace">
    <text x="118" y="538">key file</text>
    <text x="288" y="538">public JWKS</text>
  </g>

  <g fill="#3a3a3a" font-size="11">
    <text x="448" y="534">Private material reaches the process only by</text>
    <text x="448" y="547">secret reference to an owner-only file and stays</text>
    <text x="448" y="560">out of bundles, scripts, logs, and audit. Signing</text>
    <text x="448" y="573">is fail-closed; the verifier pins the key set.</text>
  </g>

  <line x1="0" y1="592" x2="720" y2="592" stroke="#e5e5e5" stroke-width="1"/>

  <g fill="#6a6a6a" font-size="9.5"
     font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace">
    <text x="16" y="609">Unauthenticated at the Evidence Gateway edge: /health, /ready, /openapi.json, /.well-known/evidence/jwks.json,</text>
    <text x="16" y="622">/.well-known/jwt-vc-issuer. None reveals which evidence definitions are enabled or which requesters may invoke them.</text>
  </g>
</svg>
