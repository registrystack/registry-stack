# Behavioral Guarantee Claims Inventory

Issue: [#206](https://github.com/registrystack/registry-stack/issues/206)

This is the extraction pass for behavioral guarantees in the public docs site.
It does not verify the claims against implementation.
Generated product docs were refreshed locally with `npm run generate`; rows for generated product pages link to the tracked source files that feed those pages.

| Product | Claim (verbatim) | Page and heading | Suggested verification | Overlap |
| --- | --- | --- | --- | --- |
| Notary | "A Notary caller never sends the value it is asking about and never receives the underlying record as the answer." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "A question goes in, an answer comes out" | New evaluation/rendering test that asserts request inputs and response bodies never carry source values for representative claim types. | None |
| Both | "there is no write-back to the source registry, and runtime services expose no data-mutation routes." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What stays home" | Code inspection of Relay and Notary route registration plus Relay integration write-surface search. | None |
| Relay | "They are never part of the public API surface, and never part of a portable metadata file that gets distributed." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What stays home" | New metadata-rendering test that sensitive runtime bindings and source paths are absent from published metadata. | None |
| Both | "The promise is *read-in-place, no write-back, retained custody*." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What this guarantees, and what it does not" | Code inspection of Relay source-capability paths, route registration, and storage/mutation boundaries. | None |
| Notary | "Across a federation boundary (one institution's Notary asking another's), what crosses is a scoped, signed evaluation result, never a credential." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What crosses the boundary" | Existing federation evaluation tests plus code inspection that federation responses cannot mint credentials. | None |
| Notary | "Unlike the plain result, the credential is cryptographically verifiable against the issuer's published keys. Credential profiles are holder-bound by default, but an operator can explicitly configure an unbound profile." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What crosses the boundary" | Existing SD-JWT VC issuance and holder-binding default tests plus operator-config warning checks for unbound profiles. | None |
| Notary | "Under the default `deny` downgrade, the service refuses a requested mode outside the allowed set; a `default` or `redacted` downgrade substitutes that fallback mode when the fallback is itself allowed." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "How much an answer reveals: the three disclosure modes" | Existing disclosure-policy tests for deny/default/redacted downgrade behavior. | None |
| Notary | "A credential is not a copy of the record. It is an **SD-JWT VC**: the signed body carries a SHA-256 *digest* of each selectively disclosable field rather than the field value, so a field the holder does not present stays hidden." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "Why the answer is not the record" | Existing SD-JWT disclosure tests and fixture inspection for undisclosed claim values. | None |
| Notary | "This is not zero-knowledge: A `predicate` answer is a policy-enforced boolean computed inside the service; SD-JWT selective disclosure is digest omission. Neither is a zero-knowledge proof." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What this guarantees, and what it does not" | Docs/source review; this is a non-guarantee boundary, not an implementation feature to assert. | None |
| Both | "This page's promise also sits inside the wider set of stack-wide limits: revocation and erasure gaps, which guarantees are left to the operator to provide, and the draft status of the underlying specifications." | [records-stay-home.mdx](../../docs/site/src/content/docs/explanation/records-stay-home.mdx), "What this guarantees, and what it does not" | Cross-check against known-limitations and product surfaces for revocation, erasure, and draft-status caveats. | None |
| Relay | "A caller without the correct `<dataset_id>:<suffix>` scope receives `403 Forbidden` before any data access occurs." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Existing Relay authorization denial tests that assert no source read on missing scope. | None |
| Relay | "Only fields declared in `entities[].fields` are projected." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Existing projection tests or new response-shape test over a source with extra columns. | None |
| Relay | "Relay builds the query from its own plan; it never forwards raw SQL or arbitrary filter expressions to the source." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Code inspection of query planning and connector boundaries. | None |
| Relay | "Raw submitted values are not written to the audit sink." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Existing audit redaction tests plus smoke grep for raw submitted values. | None |
| Relay | "Registry Platform wraps the record with `prev_hash` and `record_hash` for tamper evidence." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Existing audit envelope/hash-chain tests and Relay audit sink fixtures. | None |
| Relay | "Relay uses the secret to write stable `hmac-sha256:` handles for sensitive lookups, record identifiers, and storage table identifiers." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Per request: four phases in sequence" | Existing sensitive-field audit hashing tests plus fixture checks for lookup, record, and table handle fields. | None |
| Relay | "A data-plane scope is always bound to a specific dataset; a principal holding `social_registry:metadata` cannot access `vital_events:metadata` unless that scope is also assigned." | [consultation-flow.mdx](../../docs/site/src/content/docs/explanation/consultation-flow.mdx), "Scope model" | Existing dataset-scope authorization tests. | None |
| Notary | "The caller does not supply the evaluated value, and cannot inject one." | [disclosure-modes-and-computed-answers.mdx](../../docs/site/src/content/docs/explanation/disclosure-modes-and-computed-answers.mdx), "The evaluation pipeline: computed answers, not record handoffs" | New test that caller-supplied request attributes cannot override source-derived claim values. | None |
| Notary | "A `redacted` result goes further: it carries neither the source value nor the satisfaction outcome." | [disclosure-modes-and-computed-answers.mdx](../../docs/site/src/content/docs/explanation/disclosure-modes-and-computed-answers.mdx), "Why predicate and redacted avoid sharing the underlying record" | Existing redacted result tests, including CCCEV omission added for #176. | None |
| Notary | "By default, a matching failure collapses to a single public reason, `evidence.not_available`, so the lookup cannot be used as an existence oracle; a \"not found\" answer does not tell the caller whether a record exists." | [disclosure-modes-and-computed-answers.mdx](../../docs/site/src/content/docs/explanation/disclosure-modes-and-computed-answers.mdx), "What a redacted result still reveals (and to whom)" | Existing target/requester/relationship matching denial tests with public code assertions. | None |
| Both | "Where a source is configured to require a purpose, a request that omits the purpose header is rejected, and the supplied purpose is recorded in the audit record." | [data-minimization-and-purpose-limitation.mdx](../../docs/site/src/content/docs/explanation/data-minimization-and-purpose-limitation.mdx), "Purpose limitation as an enforced gate, not a label" | Existing PDP purpose-denial tests plus audit-event assertions. | None |
| Both | "But publishing metadata only describes; it does not authorize access, enforce a policy, or assert that any record exists, so a published manifest carries no data-protection guarantee about live data." | [data-minimization-and-purpose-limitation.mdx](../../docs/site/src/content/docs/explanation/data-minimization-and-purpose-limitation.mdx), "Data stays at source: distributed custody" | Code inspection of Manifest renderers and Relay/Notary runtime enforcement boundaries. | None |
| Both | "Every request that returns person-level records or claim results must be recorded with at least the caller principal, the scopes exercised, a request identifier, and the purpose value where one was supplied, and a deployment can run audit fail-closed so that a request whose audit record cannot be written does not succeed." | [data-minimization-and-purpose-limitation.mdx](../../docs/site/src/content/docs/explanation/data-minimization-and-purpose-limitation.mdx), "Audit as an accountability primitive" | Existing audit-context tests plus fail-closed sink failure tests across Relay and Notary. | None |
| Both | "Audit fail-closed is a capability a deployment can turn on, not a guarantee that every route in any given build has been individually audited against it." | [data-minimization-and-purpose-limitation.mdx](../../docs/site/src/content/docs/explanation/data-minimization-and-purpose-limitation.mdx), "Audit as an accountability primitive" | Code inspection and route-by-route audit coverage review. | None |
| Both | "OIDC token validation through shared platform primitives." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Existing OIDC verifier tests in shared platform crates and product auth integration tests. | None |
| Both | "Request-scope authorization and policy checks before protected reads or governed evidence evaluation." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Existing scope-denial tests and source-read denial-before-read tests. | None |
| Both | "Disclosure controls that can return narrower evidence instead of source records." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Existing disclosure-mode tests and redaction regressions. | None |
| Notary | "Credential issuance for configured Notary surfaces, including SD-JWT VC issuance where configured." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Existing credential issuance tests and profile gating tests. | None |
| Both | "Hash-chained audit records emitted by runtime services." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Existing audit envelope/hash-chain tests in platform, Relay, and Notary. | None |
| Both | "Release workflow support for public checksums, keyless cosign signatures for GitHub Release assets, image digests, image SBOMs, Grype vulnerability reports, and generated release capsules after the first capsule-producing release run." | [self-assessment.mdx](../../docs/site/src/content/docs/security/self-assessment.mdx), "Security functions and features" | Release workflow inspection plus release-script tests and published asset verification. | None |
| Both | "Second, aligning with an external standard is not the same as conforming to it: speaking the shape of OIDC, OAuth 2.0, SD-JWT VC, OID4VCI, CCCEV, or the rest does not certify conformance to any of them." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Everything here is draft" | Standards-register review plus release-docs checks for claim-level wording. | None |
| Both | "Publishing a dataset, policy, evidence offering, or federation relationship in a discovery artifact grants no access, enforces nothing, and asserts no fact about a live record." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Stack-wide boundaries at a glance" | Code inspection of Manifest rendering versus runtime authorization/PDP enforcement boundaries. | None |
| Both | "Signed config bundles are verified from a local bundle directory and activated by placing the bundle on the node and restarting; there is no admin config apply route and no hot apply." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Credential issuance and lifecycle limits (Notary)" | Code inspection of admin route registration and config-bundle startup flow. | [#314](https://github.com/registrystack/registry-stack/issues/314) |
| Notary | "No revocation flow, no issuer-metadata discovery endpoint, no erasure workflow: The RS-* specs define no revocation flow." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Credential issuance and lifecycle limits (Notary)" | Route/spec inventory for revocation, issuer metadata, and data-subject erasure surfaces. | None |
| Relay | "Relay aggregate routes return the configured statistical observations, but provide no built-in longitudinal privacy budget and do not track cumulative disclosure across repeated or overlapping queries." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Stack-wide boundaries at a glance" | Code inspection of aggregate query execution and absence of privacy-budget state. | None |
| Manifest | "Rendered standards artifacts are well-formed, not certified: Manifest rendering emits standards-shaped artifacts but does not validate them against external standard bodies: a rendered CPSV-AP, DCAT, or SHACL document is well-formed by construction, not certified against the standard." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Data-model limits (Claim and Manifest)" | Manifest renderer tests plus standards-register wording review for conformance claims. | None |
| Both | "Health checks do not certify key custody: Readiness, liveness, and protocol-conformance checks do not certify production-grade private-key custody." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Security-model operator boundary" | Posture/readiness response review to ensure no production key-custody certification is implied. | None |
| Both | "Many guarantees are the operator's to provide: Secret and key provisioning, key custody and rotation schedule, audit retention and storage, tenant isolation, transport termination, edge rate limiting, deployment configuration, and incident response are operator responsibilities, not behaviors the stack guarantees." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Security-model operator boundary" | Docs/source review of operator boundary and absence of runtime claims for deployment controls. | None |
| Both | "That is a switch a deployment is able to turn on, not a promise that every route in a given build has been individually audited, nor that every build ships with it on." | [known-limitations.mdx](../../docs/site/src/content/docs/explanation/known-limitations.mdx), "Security-model operator boundary" | Audit fail-closed configuration tests plus route-by-route audit coverage review. | None |
| Both | "This is recommended posture rather than a hard guarantee: confirm it is enabled rather than assuming it." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Threats the design considers and mitigates" | Config and posture review for outbound HTTP policy enablement; this row is a non-guarantee boundary. | None |
| Both | "Bearer tokens, private keys, source values, filesystem paths, and internal error chains stay in protected operator logs, never in responses; principal-scoped responses are not public cache entries." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Threats the design considers and mitigates" | Error-response tests, cache-control review, and redaction checks for tokens, keys, source values, paths, and internal chains. | None |
| Notary | "caller-supplied `requester`, `relationship`, and `on_behalf_of` fields are rejected before any source read, and self-asserted target fields do not satisfy trusted-principal gates." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Threats the design considers and mitigates" | Existing self-attestation/delegated-attestation denial tests that assert rejection before source access. | None |
| Both | "Known product gaps: No revocation flow is specified (though an optional, off-by-default credential-status surface can be enabled to mark a credential `revoked`), there is no `/.well-known/jwt-vc-issuer` endpoint, and no built-in data-subject erasure workflow in this version." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Residual risks and what is left to the operator" | Route inventory and docs review for revocation, issuer metadata, and erasure surfaces. | None |
| Both | "A deployment using software keys, local JWK files, or demo-generated keys can be reachable and internally consistent yet not production-secure." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Residual risks and what is left to the operator" | Readiness/posture tests for signer custody facts and explicit production gates. | None |
| Both | "Secret and key provisioning, key custody and rotation, audit retention and storage, tenant isolation, TLS termination and certificates, edge rate limiting, deployment configuration, and incident response are operator responsibilities, not behavior the model defines." | [threat-model.mdx](../../docs/site/src/content/docs/explanation/threat-model.mdx), "Residual risks and what is left to the operator" | Code inspection and docs review; this is a non-guarantee boundary. | None |
| Notary | "Registry-backed evidence enters Notary only through an authenticated, compiler-pinned Registry Relay consultation." | [README.md](../../products/notary/README.md), product introduction | Notary configuration and dependency inspection plus Relay-client integration tests that reject missing or mismatched contracts before source access. | None |
| Notary | "When both products are present, Notary receives no registry destination or credential." | [architecture-overview.md](../../products/notary/docs/architecture-overview.md), "Deployment topologies" | Configuration-schema and generated-artifact inspection proving registry destinations and source credentials exist only in Relay private inputs. | None |
| Notary | "Notary never treats a Relay failure as `no_match`." | [architecture-overview.md](../../products/notary/docs/architecture-overview.md), "Registry-backed request lifecycle" | Relay-client and claim-evaluation tests covering explicit no-match, ambiguity, denial, source, verification, availability, and contract failures. | None |
| Notary | "The handle is a hash, not the source id, so the response does not expose the raw source identifier." | [identity-and-record-matching.md](../../products/notary/docs/identity-and-record-matching.md), "What the response tells you" | Existing target reference serialization tests or new test against a source id fixture. | None |
| Notary | "The core guarantee is that a citizen token can only be used for the exact self or dependent subject authorized by policy, and only for explicitly allowed claims, purposes, formats, disclosures, and credential profiles." | [self-attestation-operator-guide.md](../../products/notary/docs/self-attestation-operator-guide.md), "Security goal" | Existing self-attestation authorization-details tests and delegated subject binding tests. | None |
| Notary | "It is not acceptable for active-active serving because two processes cannot see the same nonce or proof replay decisions." | [operator-config-reference.md](../../products/notary/docs/operator-config-reference.md), "Replay store" | Readiness/deployment-gate tests for in-memory replay under production-style profiles. | None |
| Relay | "It does not log raw query strings, request bodies, auth headers, or row values." | [ops.md](../../crates/registry-relay/docs/ops.md), "Audit sink and rotation" | Existing operational log redaction tests or new request-log fixture assertions. | None |
| Relay | "The audit record stores the approval reference, emergency change class, expiry, rate-limit identity, and hashes of approver identity and free-text `reason`. It does not store raw reason text or raw approver identity." | [api.md](../../crates/registry-relay/docs/api.md), "Listeners and surfaces" | Break-glass audit tests for hashed approver identity/reason and absence of raw values; verify against post-#314 admin route shape. | [#314](https://github.com/registrystack/registry-stack/issues/314) |
| Relay | "The initial Records surface has one collection, `datasets`, where each item is a record describing a visible `dcat:Dataset`. It uses the same metadata scopes as `/metadata/catalog`; it does not expose row data, bypass required filters, or claim searchable-catalog conformance." | [api.md](../../crates/registry-relay/docs/api.md), "Metadata, catalog, and OpenAPI" | Existing records API tests plus required-filter denial tests. | None |
| Relay | "The policy applies to every audited route. Per-route-family selection is not configurable. The selected policy is reported truthfully as the `write_policy` fact in the operations posture audit block, so a deployment cannot claim a stronger guarantee than it runs." | [configuration.md](../../crates/registry-relay/docs/configuration.md), "Write policy" | Posture schema round-trip test plus config parsing tests. | None |
| Relay | "Relay treats a workbook as immutable between reloads." | [xlsx-readiness-contract.md](../../crates/registry-relay/docs/xlsx-readiness-contract.md), "Provenance" | Existing XLSX reload/readiness tests or new test that edited workbooks require reload. | None |
| Relay | "Relay never stores it." | [deploy-standalone-with-own-data.mdx](../../docs/site/src/content/docs/tutorials/deploy-standalone-with-own-data.mdx), "Provision the API-key fingerprint" | Code inspection of registryctl generation, Relay config loading, and generated project secret handling. | None |
| Notary | "Several distinct internal conditions intentionally collapse to one public code so that the public response does not disclose more than the caller is entitled to know." | [errors.mdx](../../docs/site/src/content/docs/reference/errors.mdx), "Registry Notary" | Existing problem-code tests and audit-specific denial tests. | None |
| Both | "It does not store records or run a domain registry." | [when-to-use.mdx](../../docs/site/src/content/docs/start/when-to-use.mdx), "Page introduction" | Code inspection of product storage boundaries and source mutation absence. | None |
| Relay | "The runtime view MUST be filtered by the caller's `metadata` scope, so a caller whose scope covers one dataset does not receive metadata for datasets it cannot read." | [rs-pr-relay.mdx](../../docs/site/src/content/docs/spec/rs-pr-relay.mdx), "6. Metadata publication" | Existing metadata-scope tests. | None |
| Relay | "Relay does not expose a current CPSV-AP runtime metadata route." | [rs-pr-relay.mdx](../../docs/site/src/content/docs/spec/rs-pr-relay.mdx), "6. Metadata publication" | Route inventory check and OpenAPI/route scan. | None |
| Relay | "Relay v1 does not by itself track cumulative disclosure across repeated or overlapping aggregate queries." | [rs-pr-relay.mdx](../../docs/site/src/content/docs/spec/rs-pr-relay.mdx), "5. Aggregate routes" | Code inspection of aggregate execution and absence of disclosure-budget state. | None |
| Notary | "records and honors the disclosure mode, and never leaks a redacted value (REQ-PR-NOTARY-008, REQ-PR-NOTARY-009, REQ-PR-NOTARY-010);" | [rs-pr-notary.mdx](../../docs/site/src/content/docs/spec/rs-pr-notary.mdx), "Conformance" | Existing disclosure tests plus #176 CCCEV redaction regression. | None |
| Both | "Publishing a dataset, schema, policy, evidence offering, ecosystem binding, or federation relationship in a discovery artifact MUST NOT be construed as granting access to it, as enforcing it, or as asserting that any particular record exists or satisfies it." | [rs-arc-g.mdx](../../docs/site/src/content/docs/spec/rs-arc-g.mdx), "5. Architectural invariants" | Manifest renderer inspection plus Relay/Notary authorization and PDP boundary tests. | None |
| Manifest | "Registry Manifest MUST reject an evidence type that proves no requirement and an evidence-type-list entry that names an unknown evidence type or one that does not prove the owning requirement." | [rs-dm-manifest.mdx](../../docs/site/src/content/docs/spec/rs-dm-manifest.mdx), "5. Requirements and grouped evidence" | Existing manifest validation tests or new negative fixtures. | None |
| Manifest | "The `registry-manifest/v1` manifest and its generated `*/v1` formats MUST reject a key that is not modeled at its nesting depth, at parse time, and MUST name the offending key by its dotted field path in the resulting error." | [rs-dm-manifest.mdx](../../docs/site/src/content/docs/spec/rs-dm-manifest.mdx), "10. Compatibility and evolution" | Existing unknown-key rejection tests. | None |
| Notary | "The admin reload route returns a no-op in the standalone router; key and configuration changes still require a service restart." | [registry-notary.mdx](../../docs/site/src/content/docs/reference/apis/registry-notary.mdx), "What the specification does not cover" | Code inspection and API test for HTTP 501 `registry.admin.capability.not_supported`; wording overlaps a known docs mismatch. | [#16](https://github.com/registrystack/registry-stack/issues/16), [#314](https://github.com/registrystack/registry-stack/issues/314) |
| Notary | "Every evaluated request emits an `EvidenceAuditEvent` in a Registry Platform audit envelope to a configured sink (stdout, file, or JSONL)." | [registry-notary.mdx](../../docs/site/src/content/docs/reference/apis/registry-notary.mdx), "Behavior the schema cannot express" | Audit emission tests plus sink-list correction for syslog. | [#16](https://github.com/registrystack/registry-stack/issues/16) |
| Relay | "A route that only exists when a non-default feature or config block enables it (for example the OGC namespaces, the SP DCI adapter, or attribute release) is covered by its feature tests and product docs, not by presence in the default OpenAPI artifact. When the deployment enables it, it behaves as documented, but its availability depends on the deployment's build features and configuration." | [api-stability.mdx](../../docs/site/src/content/docs/reference/api-stability.mdx), "Covered surfaces" | Feature/config route inventory tests. | [#210](https://github.com/registrystack/registry-stack/issues/210) |
| Relay | "Registry Manifest emits CPSV-AP service catalogue JSON-LD. Registry Relay does not expose a current CPSV-AP runtime metadata route." | [glossary.mdx](../../docs/site/src/content/docs/reference/glossary.mdx), "Page introduction" | Route inventory check and OpenAPI/route scan; Manifest renderer check for CPSV-AP output. | None |

Overlap note: [#28](https://github.com/registrystack/registry-stack/issues/28) is not duplicated as an active behavioral guarantee row. The current Registry Manifest reference now lists `datasets` as `Required: No`, matching the corrected validator behavior; the old mismatch was a field-requiredness documentation defect rather than a current guarantee claim.
