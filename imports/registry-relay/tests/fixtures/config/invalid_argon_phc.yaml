# Tests config.validation_error: env var is set but value isn't a
# valid Argon2id PHC string.
server:
  bind: 127.0.0.1:8080
catalog:
  title: t
  base_url: https://example.test
  publisher: p
vocabularies: {}
auth:
  mode: api_key
  api_keys:
    - id: k1
      hash_env: TEST_KEY_HASH_BADPHC
      scopes:
        - admin
audit:
  sink: stdout
datasets: []
