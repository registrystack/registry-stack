# Tests config.duplicate_id: two datasets share the same id.
server:
  bind: 127.0.0.1:8080
catalog:
  title: t
  base_url: https://example.test
  publisher: p
vocabularies: {}
auth:
  mode: api_key
  api_keys:
    - id: k1
      hash_env: TEST_KEY_HASH_DUP
      scopes:
        - registry_relay:admin
audit:
  sink: stdout
datasets:
  - id: ds_one
    title: T
    description: D
    owner: O
    sensitivity: public
    access_rights: public
    update_frequency: monthly
    conforms_to: []
    defaults:
      refresh:
        mode: manual
    tables:
  - id: ds_one
    title: T2
    description: D2
    owner: O2
    sensitivity: public
    access_rights: public
    update_frequency: monthly
    conforms_to: []
    defaults:
      refresh:
        mode: manual
    tables:
