# Registry Relay Agent Skills

Registry Relay Codex skills are now maintained in the public
`registry-skills` plugin repository.

Canonical skills:

- `registry-relay-config-author`
- `registry-relay-config-review`

Use the `registry-skills` plugin instead of editing repo-local copies here.
