# Registry Platform

Shared security and operational primitives for Registry Relay and Registry Witness.

This workspace is consumed by the applications through a pinned git tag. It owns
HTTP outbound policy, auth primitives, OIDC verification, audit chaining,
browser-facing HTTP security helpers, SD-JWT helpers, crypto primitives, and
test fixtures.
