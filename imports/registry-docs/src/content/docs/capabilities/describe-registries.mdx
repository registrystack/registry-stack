---
title: Describe registries
description: How Registry Manifest describes datasets, entities, policies, forms, evidence offerings, and standards artifacts without running a service.
status: current
owner: registry-legend
source_repos:
  - registry-manifest
  - registry-relay
  - registry-atlas
last_reviewed: "2026-05-28"
doc_type: explanation
locale: en
standards_referenced:
  - dcat
  - bregdcat-ap
  - cpsv-ap
  - cccev
  - shacl
  - json-schema
  - odrl
  - ogc-api-records
---

Describe registries is how a registry owner publishes a machine-readable description of what
they offer, datasets, schemas, services, policies, and evidence offerings, before any
integration is written. The primary implementation is
[Registry Manifest](../../projects/registry-manifest/), which compiles a portable
`metadata.yaml` manifest into catalogs, schemas, service descriptions, policy documents,
evidence offerings, and an `index.json` discovery entry point.

## What it produces

Registry Manifest renders:

- A DCAT/BregDCAT-AP catalog and dataset entries.
- CPSV-AP service description.
- CCCEV evidence offering definitions.
- SHACL and JSON Schema shape files.
- ODRL policy documents.
- An `index.json` discovery entry point consumed by Registry Atlas.

Registry Relay can also serve Manifest-rendered metadata at runtime, filtered to the authenticated
caller's metadata scopes.
Registry Manifest does not read registry records, open an HTTP listener, or grant credentials.

## Primary project

[Registry Manifest](../../projects/registry-manifest/): renderer, validation, and profile fixture model.

## How to use it

Start with the [manifest schema reference](../../projects/registry-manifest/reference/).
To publish, run the Manifest compiler against your `metadata.yaml`.
To inspect the output, use [Registry Atlas](../../projects/registry-atlas/).
To serve runtime metadata to authenticated callers, configure [Registry Relay](../../projects/registry-relay/).

Example: a property registry run by a national land authority publishes a manifest declaring its
`parcels` dataset, the parcel-identifier field, the freshness window, and the evidence offerings
available against it.

## Related docs

- [Publishing pipeline](../../explanation/publishing-pipeline/): portable metadata and runtime
  metadata split.
- [Registry Manifest overview](../../projects/registry-manifest/): renderer, validation, and profile
  fixture model.
- [Registry Manifest reference](../../projects/registry-manifest/reference/): manifest schema and
  publish output artifacts.
- [Registry Relay overview](../../projects/registry-relay/): runtime metadata publication through
  protected services.
- [Standards reference](../../reference/standards/): standards named by the metadata surface.
