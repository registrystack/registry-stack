---
title: Standards register
description: Standards referenced by the registry family, with explicit claim levels and supporting evidence.
wide: true
status: current
owner: registry-legend
source_repos:
  - registry-relay
  - registry-manifest
  - registry-witness
  - registry-atlas
last_reviewed: "2026-05-23"
doc_type: reference
locale: en
standards_referenced:
  - dcat
  - bregdcat-ap
  - ogc-api-records
  - ogc-api-features
  - openapi
  - shacl
  - json-schema
  - json-ld
  - sd-jwt-vc
  - verifiable-credentials
  - cccev
  - odrl
  - prov-o
  - govstack-digital-registries
  - sp-dci
  - w3c-did
---

import StandardsTable from '../../../components/StandardsTable.astro';

This register is the source of truth for standards claims in Registry Legend.
The registry family does not claim to be a new standard.
Each project implements specific surfaces around existing standards and declares that use with
an explicit claim level.

The table is generated from `src/data/standards.yaml`.

## Claim levels

Six levels are used, in descending strength:

- `implements` means the project implements a normative requirement or API shape from the standard.
- `emits` means the project produces artifacts shaped like the standard, such as JSON-LD, SHACL, OpenAPI, or catalog records.
- `maps_to` means local concepts or fields are mapped to the standard.
- `aligns_with` means the project follows the model or intent without claiming formal conformance.
- `inspired_by` means the standard influenced design without any compatibility claim.
- `compares_against` means the standard is discussed to explain boundaries or alternatives.

A claim is only as strong as its evidence.
When evidence is a README sentence, the claim is at most `aligns_with`.
When evidence is a test, fixture, or generated artifact, the claim can reach `emits` or `implements`.

## Column guide

- **Standard** links to the official standards body page.
- **Status** is one of `used`, `referenced`, `evaluated`, `planned`, or `historical`.
- **Used by** lists the registry family projects that reference this standard.
- **Claim level** uses the six levels defined above.
- **Surface** names the specific output or endpoint that the claim applies to.
- **Profile and notes** identifies the version or profile in use and any boundary conditions.
- **Evidence** links to the source code, fixture, or document that supports the claim.

## Register

<StandardsTable />

## Gaps

- OGC API Features is listed as `aligns_with` (`referenced`).
  Route evidence has not been pinned to a specific commit.
  Promote to `emits` only after `src/api/ogc/features.rs` is exercised by an integration test.
  {/* TODO[evidence]: relay evidence packet notes "feature-gated; GeoJSON support for spatial features" but no integration test fixture found at commit aec4394. */}
- The Verifiable Credentials Data Model (W3C VC) is listed as `aligns_with` (`referenced`).
  Registry Relay has VC-oriented provenance fixtures and Registry Witness issues SD-JWT VC credentials,
  but neither project emits a full W3C VC Data Model envelope.
  Promote when a reviewed VC profile and validation fixtures are pinned.
  {/* Witness evidence packet explicitly states "W3C Verifiable Credentials Data Model: NOT PRESENT" (.research/registry-witness.md). This is a confirmed absence, not an evidence gap; keep at aligns_with until a reviewed VC profile and validation fixtures are pinned. */}
- PROV-O is listed as `inspired_by` (`referenced`).
  The code uses provenance-shaped concepts (audit fields, claim provenance struct) but no PROV-O
  vocabulary terms are emitted as JSON-LD.
  {/* Confirmed absence: no `prov:` namespace or PROV-O `@context` found in relay or witness source at reviewed commits. PROV-O influences the design (audit fields, claim provenance struct) but no PROV-O vocabulary is emitted. Keep at inspired_by. */}
