---
title: Project map
description: The five registry family projects, their responsibilities, and the flow across them.
wide: true
status: current
owner: registry-legend
source_repos:
  - registry-relay
  - registry-manifest
  - registry-witness
  - registry-atlas
  - registry-lab
last_reviewed: "2026-05-23"
doc_type: explanation
locale: en
standards_referenced:
  - OpenAPI
  - DCAT
  - SHACL
  - JSON-Schema
  - SD-JWT-VC
---

import ProjectMap from '../../../components/ProjectMap.astro';

The registry family has five repositories with distinct responsibilities.
Use this map to decide which repository owns a question before opening implementation docs.

<figure>
  <img src="/registry-legend/images/registry-family-map.svg"
       alt="Five registry family projects and how they connect. Registry Manifest compiles portable metadata. Registry Relay binds metadata to runtime sources and exposes protected APIs. Registry Witness evaluates claims and issues credentials. Registry Atlas inspects published artifacts in a browser. Registry Lab runs all of these together as a compose topology." />
</figure>

<ProjectMap />

{/* Generated from src/data/projects.yaml by the ProjectMap component.
    Update projects.yaml to change any row; do not hand-edit the table above. */}

## Flow across the family

Registry Manifest defines portable metadata and compiles it into standards-facing artifacts.
Registry Relay binds logical datasets and entities to protected runtime sources and exposes them as authorized read-only APIs.
Registry Witness evaluates configured claims against registry sources and issues SD-JWT VC credentials.
Registry Atlas inspects published discovery artifacts, parses DCAT catalogs and capability envelopes, and answers strict capability queries.
Registry Lab runs the smallest local ecosystem demo, composing three Relay authorities, three Witness services, a static metadata publisher, and a narrated client.

## Rename status

Current repo slugs are `registry-relay`, `registry-manifest`, `registry-witness`, `registry-atlas`, and `registry-lab`.
The 2026-05-23 rename plan resolved the two legacy names:

- `registry_relay` (underscore form) is complete as of v0.1.0; the public-facing binary, crate, and repo all use the hyphen form `registry-relay`.
- `decentralized-evidence-demo` has been renamed to `registry-lab`. Some scripts and fixtures inside the repo still reference the old name in identifiers and docstrings; those internal references are intentionally stable and do not affect the repo slug or product label.

Until the rename finishes propagating through all linked docs, Registry Legend uses target product labels in explanatory prose and current local paths in evidence links.

---

**Next:** [Ownership boundaries](./ownership-boundaries/): which responsibility each project does not own.

{/* Editor note:
    - Rewrote 2026-05-23. Promoted to status: current 2026-05-23.
    - SVG (public/images/registry-family-map.svg) restored with descriptive alt text.
    - source_repos updated: replaced `registry_relay` and `decentralized-evidence-demo`
      with `registry-relay` and `registry-lab`.
    - The "Rename Status" content was condensed into prose grounded in the registry-relay
      and registry-lab evidence packets.
    - The ProjectMap component renders from src/data/projects.yaml; no hand-written table added.
    - Flow paragraph verified against all five evidence packets. */}
