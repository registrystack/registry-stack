---
title: Verify a business registry status without sharing the full record
description: Return a narrow status fact about a registered company without disclosing the full business record.
status: current
owner: registry-legend
source_repos:
  - registry-witness
  - registry-relay
  - registry-manifest
last_reviewed: "2026-05-28"
doc_type: explanation
locale: en
standards_referenced:
  - bregdcat-ap
  - dcat
  - openapi
  - cccev
  - sd-jwt-vc
  - oid4vci
  - w3c-did
  - json-ld
---

## Starting situation

A national business registry records company incorporations, legal-status changes, and
gazette references in a relational database.
A public procurement platform must confirm that a supplier is actively registered before
it processes a tender submission.
The procurement platform needs a yes/no answer tied to the national business number.
It does not need the company's beneficial-ownership chain, registered address, or filing
history.

## What you set up

Configure a Registry Witness claim for the active-status fact.
The claim definition names the source binding (the business registry table or extract),
the rule (registration status equals "active"), the disclosure mode (predicate: true/false),
and the issuer identifier.

Configure Registry Relay to expose the consultation route that accepts a national business
number or LEI and routes the request to Witness for evaluation.
Use Registry Manifest to publish BRegDCAT-AP-shaped catalog metadata for the business registry,
including a link to the evidence offering document that describes the active-status claim.

## What a caller does

The procurement platform sends an authorized POST to the Witness evaluation endpoint,
passing the national business number as the subject identifier and naming the active-status
claim.
The platform authenticates with the configured identity provider.

For a credential issuance flow, the supplier (or an agent acting on their behalf) receives an
SD-JWT VC from Witness. The supplier presents only the active-status disclosure to the
procurement platform, keeping unselected credential fields hidden.

## What the caller gets back

A signed evaluation containing: the national business number, the predicate result (active: true
or false), the evaluation timestamp, and the issuer identifier.

The full company record, beneficial-ownership chain, gazette reference list, registered address,
and internal registry notes do not appear in the response.

For a dissolved or suspended company, the predicate returns false.
The specific legal-status code (suspended, dissolved, struck off) can be included as a separate
configured disclosure if the governing policy permits it.

## What is in scope, what is not

This pattern covers: evaluating a narrow company-status fact against the business registry,
returning a predicate or signed credential, and publishing BRegDCAT-AP metadata that makes the
offering discoverable.

The registry subject is a legal entity identified by a national business number or LEI.
The relevant facts are incorporation status, active/suspended/dissolved status, registration
class, and gazette references where the governing policy permits them.

This pattern does not cover: full company record sharing, beneficial-ownership disclosure beyond
what a configured claim permits, dynamic trust-chain federation, or automatic legal-basis
approval.
The issuing institution owns the rule definition, the audit trail, the correction process, and
the terms under which a relying party may request the fact.

## Next

- [Evidence issuance, end to end](../../explanation/evidence-issuance/)
- [Registry Witness overview](../../projects/registry-witness/)
- [Publishing pipeline](../../explanation/publishing-pipeline/)
- [Publish registry metadata](../publish-registry-metadata/)
