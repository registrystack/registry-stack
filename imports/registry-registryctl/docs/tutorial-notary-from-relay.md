# Verify a claim from your registry API

This tutorial is published in Registry Docs:

<https://docs.registrystack.org/tutorials/verify-claim-registry-api/>

`registry-registryctl` keeps the CLI source, installer, generated templates, and command reference.
Registry Docs owns the beginner tutorials so the onboarding path is not split across product
repositories.
