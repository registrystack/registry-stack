# The shape extraction must produce before the derivation reads it. It is a
# closed shape on purpose: a fact the question never declared cannot reach the
# derivation by being added here.
type: object
additionalProperties: false
required: [status]
properties:
  status:
    type: string
    minLength: 4
    maxLength: 32
