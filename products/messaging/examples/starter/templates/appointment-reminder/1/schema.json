{
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "day", "office"],
  "properties": {
    "name": {"type": "string", "minLength": 1, "maxLength": 200},
    "day": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"},
    "office": {"type": "string", "minLength": 1, "maxLength": 200}
  }
}
