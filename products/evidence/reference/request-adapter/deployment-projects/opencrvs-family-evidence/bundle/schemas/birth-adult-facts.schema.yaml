type: object
additionalProperties: false
required: [record_tracking_id, date_of_birth]
properties:
  record_tracking_id:
    type: string
    minLength: 1
    maxLength: 64
  date_of_birth:
    type: string
    format: date
