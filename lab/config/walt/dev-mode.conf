# Registry Lab change: walt's stock value is `false`, which resolves did:web
# over plain HTTP. The credentials issued by the citizen Notary are signed by
# `did:web:citizen-notary.lab.registrystack.org`; the wallet resolves that DID
# (https://citizen-notary.lab.registrystack.org/.well-known/did.json) to verify
# and display the credential, so did:web resolution must use HTTPS.
enableDidWebResolverHttps=true
